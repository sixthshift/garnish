# Only what `bun run build` needs reaches the build context.
node_modules
.output
data
.git
test
docs

# framework caches and local tooling
.nitro
.tanstack
.vite
.devcontainer
*.db
*.db-shm
*.db-wal
Dockerfile
.dockerignore
