# Copy to .devcontainer/.env — that file is gitignored.
# Nothing is required yet; the app stores data in SQLite (no DB credentials).
#
# ANTHROPIC_API_KEY=   # for LLM-based recipe import, once that lands
