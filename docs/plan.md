# Plan

Implementation plan for garnish, stages 7 to 12 (what surrounds the recipe: the shopping list, sub-recipes and conversions, the meal plan, export and import, a bundle of small parity items, and a second pass over the import), written to be executed one task per loop iteration with no human present. Decisions live in [decisions.md](decisions.md), shape in [architecture.md](architecture.md), boundaries in [scope.md](scope.md). Run it with `/ailoop` (see `.claude/skills/ailoop/SKILL.md`): one fresh subagent per task, the orchestrator verifies and records. This file is edited only by the orchestrator. Completed stages live in `docs/plans/`; stage 1 is [v1-foundations.md](plans/v1-foundations.md), stage 2 [v2-ui.md](plans/v2-ui.md), stage 3 [v3-parsing.md](plans/v3-parsing.md), stage 4 [v4-editor.md](plans/v4-editor.md), stage 5 [v5-recipe-page.md](plans/v5-recipe-page.md), stage 6 [v6-step-card.md](plans/v6-step-card.md).

## Loop protocol

What one task looks like, whoever runs it (an ailoop subagent, a /loop firing, or a person):

1. Read this file. Read `CLAUDE.md`. Do not re-read the other docs unless a task points at them.
2. Pick the first unchecked task whose dependencies are checked. Milestones run in order; tasks within a milestone run in order unless marked `∥`. A `!` before the task id asks `/ailoop` to dispatch it on a stronger model; the skill's Model section has the rules.
3. Do that one task. Nothing else. No drive-by refactors, no adjacent tasks.
4. Verify with the task's own check. Then run the full gate:
   ```
   bun run check     # tsc --noEmit
   bun run test      # vitest run
   ```
   Both must pass. A failing gate is not done. Fix it or revert the task.
5. Commit with a one-line message naming the task id, e.g. `M1.3 recipe document schema`. Attribution trailer per `CLAUDE.md`.
6. Report. Under `/ailoop` the orchestrator ticks the task, appends the Log line and commits; a subagent never edits this file. Under `/loop`, do it yourself.
7. Stop. One task per iteration.

Rules:

- **Blocked?** Write the blocker under Blocked with the task id and what you tried. Move to the next task that does not depend on it. Never guess past a blocker on a decision; guess freely on implementation detail, and when unsure copy Mealie.
- **Question for Jason?** Write it under Questions. Pick the Mealie answer and continue. Do not stop.
- **Found a design gap?** Add a row to `decisions.md`. Do not edit existing rows. Note it in the Log.
- **Framework questions?** TanStack Start docs at https://tanstack.com/start/latest. Server functions for app calls, server routes only under `src/routes/api/`. SPA mode stays on.
- **UI element needed?** Check the design system's exports first (`node_modules/@sixthshift/design-system/package.json`). Build locally only if absent, under `src/components/ui/`.
- **Task too big for one iteration?** Split it in place into `Mx.y.a`, `Mx.y.b`. Do the first part.
- **Tests:** every pure function and every API route gets tests in the same task that creates it. No task is done with a TODO test.
- **No new dependencies** beyond the list in M0 without a decisions.md row saying why.

## Layout

```
src/
  routes/       TanStack Start file routes: pages, and server routes under routes/api/
  server/       server functions (createServerFn), grouped by resource; db access only here
  db/           migrations/*.sql, migrate.ts, seed.ts, repositories
  domain/       zod schemas, scaling, formatting. Pure, no IO, importable by client
  components/   React components; ui/ holds local primitives the design system lacks
  lib/          client-side helpers (mutate + router.invalidate)
  styles.css    Tailwind entry with the design system's three lines
  router.tsx    getRouter()
vite.config.ts  tanstackStart({ spa }), nitro({ preset: 'bun' }), viteReact(), tailwindcss()
test/           mirrors src/
data/           runtime volume: garnish.db, images/, backups/  (gitignored)
```

Scripts in `package.json`: `dev` (`bun --bun vite dev`), `build` (`bun --bun vite build`), `start` (`bun run .output/server/index.mjs`), `check` (`tsc --noEmit`), `test` (`vitest run`), `migrate`, `seed`, `backup`.


## Stages 7 to 12: around the recipe

Six stages built the recipe: the model, the parser, the editor, the import, the page, the step card. It is at parity with Mealie and Tandoor or ahead. What both incumbents have that garnish does not is everything around the recipe, and the schema has anticipated most of it since stage 1: `aisle`, `food.skip_shopping`, `unit.standard_*`, `food.recipe_id`, the merge rule in decision 13, the disabled "Add to shopping list" button from M30.4.

The order, and the garnish-shaped version of each:

- **Stage 7, the shopping list.** One list, no owner. Merge by food and unit, expand a line to its sources, group by aisle. Add from the recipe at the current scale. The garnish-specific problem: the supermarket is not on the LAN, so the list must open and tick offline. That is the app's first queued write.
- **Stage 8, sub-recipes and conversions.** A food that points at a recipe renders as a link with a scale hint; conversions per food so a cup and 300 g of flour merge; then the list scales through a sub-recipe. Conversions before expansion, or the list gets two flour lines.
- **Stage 9, the meal plan.** A week strip, a recipe or a line of text on a day, one tap adds the week to the list. No meal types, no per-person plans.
- **Stage 10, out and in.** JSON and Cooklang export, import from Mealie and Tandoor backups (the adoption path neither incumbent offers), then `claude -p` import as the rung under JSON-LD, landing on the existing review step.
- **Stage 11, small parity items.** Step images, servings on a cook log entry, an ingredient preview on the card, a fetch that gets past a bot wall. Independent of each other and of the stages above; `∥`.
- **Stage 12, the import pipeline.** Stage 10 built the rungs; this makes them one pipeline with a language model as the reader. schema.org has no place for which part an ingredient belongs to, so on any page with sections the rules produce the wrong shape by construction, and the JSON-LD's real value is as ground truth rather than as the answer. After this, every page is read by the model when one is configured, with the JSON-LD as its anchor: the page owns the lines and the steps, the model supplies the parts, and a mechanical check discards any answer that changed the content. The rules result shows meanwhile and is what you get when no model is configured, or it fails, or it is rejected. The model is a hosted one behind one OpenAI-compatible HTTP call, configured by environment, defaulting to Gemini's free tier; `claude -p` goes. Pasted HTML goes through the same path as a fetched page.

Migrations run 005 to 009 in stage order. Each new table gets a Drizzle mirror and the drift test. Each stage's first task carries the decisions row for its model.

Not in these stages, and not to be re-raised without a decisions row: nutrition and costing, cookbooks or any second organiser beside tags (decision 42), comments, share links, categories and tools, per-person or per-meal-type planning, inventory (intent.md), and a DOM test environment (the stage 5 question still stands for Jason).

## M31 The shopping list

- [x] **! M31.1 The list's tables.** `src/db/migrations/005_shopping.sql`: `shopping_item (id, position, food_id? SET NULL, unit_id? SET NULL, quantity REAL?, text TEXT NOT NULL DEFAULT '', ticked INTEGER NOT NULL DEFAULT 0, created_at, updated_at)` and `shopping_item_source (id, item_id CASCADE, recipe_id? SET NULL, recipe_name TEXT, part_name TEXT, servings REAL?, quantity REAL?)`, so a line remembers where it came from even after the recipe is deleted. Drizzle mirror in `src/db/models/shopping/schema.ts`, repository in `repo.ts` (list, add many, update, tick, remove, clear ticked, reorder), document in `src/domain/shopping.ts` (zod, camelCase, `sources` nested), server functions in `src/server/shopping.ts`. `architecture.md` gains a Shopping list section replacing the design-only one; decisions.md row (one household list, items keep their provenance). Check: migrate fresh and existing; repository round-trip tests including a source surviving its recipe's deletion; the drift test.
- [x] **M31.2 Merging into the list.** `src/domain/shopping.ts` gains `mergeIntoList(items, additions)`: an addition with the same food and unit as an unticked item adds its quantity and appends its source; a ticked item is left alone and a new line is made; a free-text addition never merges; a `skipShopping` food is dropped; a text-only ingredient (no food) becomes a free-text line with its `originalText`. Pure, reuses `mergeIngredients`'s rules where they match. Check: a table test per rule, and one over a whole recipe at scale 2.
- [x] **! M31.3 Add from the recipe.** The disabled button from M30.4 goes live: a `sheet` listing the recipe's ingredients at the page's current scale, every row ticked to include, tap to exclude, with the part name as a group heading on a multi-part recipe; **Add** runs M31.2 against the current list through `addShoppingItems` and toasts "N items added" with a link to the list. Cook mode's finish card gets the same button. Check: render tests for the sheet; a test that Add sends only the included rows at the scaled quantities with the recipe as source.
- [x] **! M31.4 The list page.** `/shopping`: a nav item between Recipes and Settings. Items grouped by aisle in `aisle.position` order, unassigned last, each group a heading; a row is a tick box, amount and food (or the text), and a chevron that expands the sources ("Lemon Tart, Pastry, serves 4"); ticked rows sink to a "Ticked" group at the foot with **Clear ticked**. A free-text input at the top adds a line on Enter. Phone first: rows are tap targets, nothing hover-only. Check: render tests for grouping, the sources expansion, the ticked group and the empty state.
- [x] **! M31.5 Ticks that work in the supermarket.** The list must open and tick with no server. `src/lib/outbox.ts`: a queue of pending writes (tick, untick, remove) in `localStorage`, applied to the rendered list optimistically and flushed in order when `useOnline` turns true or the page regains focus; a flush failure keeps the entry and shows "N changes waiting" in the page header. The service worker's data cache already keeps the last list read. `RecipeForm`'s "writes are never attempted offline" stays true for everything else; the list is the one exception and the row says why. decisions.md row. Check: tests over the pure queue (enqueue, coalesce a tick and untick of the same item, flush order, a failed flush retained); a render test that an offline tick shows ticked and queues.
- [x] **M31.6 Aisles from the list.** A row whose food has no aisle shows a quiet "Set aisle" in its expansion, a `Select` over the aisles writing `updateFood`; the row moves group on the next read. Foods and aisles stay managed in Settings as today. Check: render test for the control's presence only on unassigned foods; a test that choosing writes the food.

## M32 Sub-recipes and conversions

- [x] **! M32.1 Conversions per food.** `src/db/migrations/006_conversions.sql`: `food_conversion (id, food_id CASCADE, unit_id CASCADE, quantity REAL NOT NULL, to_unit_id CASCADE, to_quantity REAL NOT NULL, UNIQUE (food_id, unit_id, to_unit_id))`, read as "1 cup of flour is 125 g". Drizzle mirror, repository under `src/db/models/food/`, the food document gains `conversions: []`, server functions on `foods`. The food edit sheet in Settings gains a conversions editor: rows of quantity, unit, equals, quantity, unit. decisions.md row (conversions live on the food, not the unit; `unit.standard_*` stays for unit-to-unit like ml to l). Check: migrate; repository round-trip; render test for the editor.
- [x] **M32.2 Convert.** `src/domain/convert.ts`: `convert(quantity, unit, food, toUnit)` through the food's conversions, then `unit.standard_*` for unit-to-unit, chaining one hop each way; null when no path. `mergeIntoList` (M31.2) uses it: two lines for one food in convertible units merge into the metric one. Pure. Check: a table test over direct, reverse, one-hop chained, and no path; a merge test that a cup and 300 g of flour become one line.
- [x] **! M32.3 A food made by a recipe.** `food.recipe_id` becomes editable: the food edit sheet in Settings gains "Made by a recipe", a `Combobox` over recipes by name; the recipe page's action menu gains "Make this a food", which creates or links a food of the recipe's name with `recipeId` set. An ingredient row whose food has a recipe renders the food as a link to it, with a hint from `subRecipeScale(ingredient, child)` in `src/domain/subRecipe.ts`: the servings of the child that yield the ingredient's amount, through the child's `recipeYieldQuantity` and `yieldUnit` and M32.2's `convert`; null when they cannot be related, and then the link has no hint. decisions.md row (a link with a scale hint; the parent never inlines the child). Check: tests for `subRecipeScale` over matching units, convertible units and unrelated ones; render tests for the row link and hint.
- [x] **M32.4 Cook through it.** In cook mode, a step card whose linked ingredient is a sub-recipe shows "Open <child> at N servings" under the row, a link into the child's cook mode with `?servings=`; the child's finish card offers "Back to <parent>" when it was entered that way (`?from=` slug). Check: render tests for the link and the way back.
- [x] **M32.5 The list scales through it.** Adding a parent recipe to the list (M31.3) offers, per sub-recipe row, "Add hollandaise's ingredients instead", which adds the child's ingredients at the servings M32.3 derived; without a derivable scale the food itself is added as a line. Check: a test that the child's rows arrive scaled with the child as source; a test that an underivable scale adds the food line.

## M33 The meal plan

- [x] **! M33.1 The plan's table.** `src/db/migrations/007_plan.sql`: `meal_plan_entry (id, date TEXT NOT NULL (YYYY-MM-DD), position, recipe_id? SET NULL, text TEXT NOT NULL DEFAULT '', servings REAL?)`, so a day holds recipes or plain lines ("leftovers"). Drizzle mirror, repository under `src/db/models/plan/` (a week's entries, add, update, move, remove), document and server functions. decisions.md row (one plan, days not meals). Check: migrate; round-trip; the drift test.
- [x] **! M33.2 The week.** `/plan`: a nav item. Seven columns from `md`, a vertical list of days below it, today marked, arrows for the previous and next week (`?week=` as the Monday's date). A day has its entries as small cards (image, name, servings) and an add row: type to search recipes (the `GlobalSearch` list reused) or press Enter on unmatched text for a plain line. Entries move between days by drag (`ReorderList`'s group drag) or a row menu. Check: render tests for the week, an entry of each kind, and the empty day.
- [x] **M33.3 The week to the list.** "Add this week to the shopping list" in the plan's header runs M31.2 over every recipe entry at its servings (the recipe's own when unset), with the plan day as part of the source. Check: a test over a week with two recipes and a text line.
- [x] **M33.4 Plan from the recipe.** The recipe page's action menu gains "Plan", a popover with the next seven days and a servings stepper defaulting to the page's scale. Check: render test; a test that choosing a day writes an entry.

## M34 Out and in

- [x] **M34.1 JSON export.** `GET /api/recipes/:slug.json` returns the recipe document; `GET /api/export.json` returns every recipe's document plus the foods, units, aisles and tags, images referenced by their `/api/images/` URLs. Settings gains an Export tab with a download link and a note that images are not in the file. decisions.md row (reverses decision 4's "no file export in v1" now that v1 is done; the database stays the master). Check: route tests for both, including a recipe with links and a sub-recipe.
- [x] **M34.2 Cooklang export.** `src/domain/cooklang.ts`: `toCooklang(recipe)`: metadata block (servings, source, tags), a `== Part ==` section per named part, steps with `@food{qty%unit}` where the step links the row and the plain text otherwise, `=` for fixed quantities, timers as `~{20%minutes}` from `durationsIn`. Pure. `GET /api/recipes/:slug.cook` serves it; the recipe menu gains "Copy as Cooklang". Check: a golden-file test over the three sample recipes; a property test that every linked food appears as an `@` reference.
- [x] **! M34.3 Import a Mealie backup.** The source chooser gains a third option, **a Mealie or Tandoor export**, taking a file. `src/domain/importMealie.ts`: reads Mealie's backup zip or a single recipe JSON; `recipeIngredient` rows with a `title` open a new part, as do `recipeInstructions` with one; `food`/`unit`/`quantity`/`note` map straight across, `originalText` kept; `tags` and `categories` both become tags; `notes`, `rating`, `recipeYield`, times, `orgURL` to `sourceUrl`; the image is fetched from the zip. Steps get links through `suggestLinks`. Reviewed through the M17.5 rows before anything is written; duplicates by name are warned as M23.7 warns by URL. Check: a fixture from Mealie's documented format with sections; a test that a `title` becomes a part.
- [x] **! M34.4 Import a Tandoor export.** `src/domain/importTandoor.ts` for Tandoor's JSON export: each `step` with ingredients becomes a part named from the step's `name` (or the unnamed body when blank), its `instruction` the part's one step, its ingredients the part's rows already linked to that step; `keywords` become tags; `servings`, `working_time`, `waiting_time`, `source_url`, `description`; nested-recipe steps become a food with `recipeId` when the child is in the same export, else a text line. Same chooser, same review. Check: a fixture with two steps and a nested recipe.
- [x] **! M34.5 `claude -p` import.** The rung under JSON-LD and OpenGraph (decision 58) for pasted prose and pages with no structured data: `src/server/aiImport.ts` runs `claude -p` through `Bun.$` with the page text or the paste and a strict JSON schema for `ScrapedRecipe` (`--output-format json`), 60 s timeout, and hands the result to the same review the URL import uses. Shown only when the `claude` binary is on the path (`Bun.which`), with a Settings note on `claude setup-token` for the container (decision 12). The paste box from decision 52 returns as the entry point, gated the same way. decisions.md row (supersedes decision 10's "no AI in v1"; row 11's `claude -p` on subscription stands). Check: a test with the runner injected returning a fixture; a test that a missing binary hides the option; a test that a malformed answer is reported, not saved.

## M35 Small parity items

- [x] **∥ M35.1 Step images.** `src/db/migrations/008_step_images.sql` adds `step.image TEXT?`; `POST /api/steps/:id/image` and `GET /api/images/steps/:file` as the recipe and timeline pairs are; the step row menu in the editor gains "Add image"; `StepCard` shows it above the text at both sizes. Check: route tests; a render test at both sizes.
- [x] **∥ M35.2 Servings on a cook.** `009_cook_servings.sql` adds `timeline_event.servings REAL?`; the Made this sheet gains a servings stepper defaulting to the page's scale; the History row shows "serves 6" when set. Check: round-trip; render tests.
- [x] **∥ M35.3 Ingredients on the card.** From `md`, hovering a recipe card shows its first six ingredient lines in a `Tooltip`; the summary gains `ingredientPreview: string[]` from the repository. Check: repository test; render test.
- [x] **∥ M35.4 Past the bot wall.** `importFromUrl` sends a full browser header set (`Accept`, `Accept-Language`, `Sec-Fetch-*`, a current Chrome `User-Agent`) and, on a 403, retries once with a second profile; still blocked, the error names the site and suggests the paste box. Recorded 403 and 200 fixtures. Check: tests for the retry and the message. Note in the task's Log whether Serious Eats now passes; TLS fingerprinting is out of reach for `fetch` and stays deferred.

## M36 The import pipeline

What `recipe_scrapers` gets right is one rule, in its ingredient-group reader: the JSON-LD owns the lines, the HTML only says which heading each sits under, and a count mismatch throws the grouping away. This milestone builds that rule with a language model in place of per-site selectors, and makes the model the default reader of a page rather than the fallback under the rules: the rules cannot allocate ingredients to parts, and the format needs that. The model is a hosted one behind a single OpenAI-compatible HTTP call, not `claude -p`: the task does not need frontier capability, the CLI is a bespoke integration on a moving target, and a container should not need a login step. Order matters: M36.1 swaps the backend every later task talks to, and M36.2 changes the import document every later task reads. `ImportedRecipe` gains two fields along the way: `pageText` (M36.3) and `check` (M36.5).

- [x] **! M36.1 One HTTP call to a hosted model.** `src/server/aiImport.ts` drops the `claude` binary: `claudePath`, `CLAUDE_BINARY`, `claudeArgs`, `spawnRunner` and the CLI envelope parsing go. In their place `fetchRunner` POSTs to `${AI_BASE_URL}/chat/completions` with `Authorization: Bearer ${AI_API_KEY}`, body `{ model: AI_MODEL, messages: [{ role: "user", content: prompt }], response_format: { type: "json_schema", json_schema: { name: "recipe", schema: SCRAPED_JSON_SCHEMA, strict: true } }, temperature: 0 }`, `AbortSignal.timeout(AI_IMPORT_TIMEOUT_MS)`, and returns `choices[0].message.content`; `parseAiAnswer` reads that string (keeping `stripFence` for models that fence anyway). `AiRunner` becomes `(prompt, timeoutMs) => Promise<string>` and stays injectable. `aiConfigured()` is `AI_API_KEY` being set; `AI_BASE_URL` defaults to Gemini's OpenAI-compatible endpoint and `AI_MODEL` to its current Flash model, so the free tier is one variable, and Mistral, Groq, OpenRouter or a LAN Ollama are a base URL and a model name away (Ollama takes any non-empty key). Errors keep their four kinds: no key is `unavailable`; a 401/403 says the key was rejected, a 429 says the provider is rate-limited and to try again in a minute, another non-2xx names the status (`failed`); the abort is `timeout`; a non-recipe answer is `malformed`. Plain `fetch`, no SDK. `aiImportAvailable` reads `aiConfigured()`. `docker-compose.yml` passes the three variables through from the host (`${AI_API_KEY:-}` and so on; `test/docker/compose.test.ts` follows) and README gains an AI section. The Settings note replaces `claude setup-token` with the three variables and one line that the Gemini free tier may train on what is sent, which for a public recipe page costs nothing. `CLAUDE.md`'s and `AGENTS.md`'s AI line, `intent.md` line 48, `scope.md` line 19 and `architecture.md`'s import line say "a hosted model over an OpenAI-compatible endpoint" instead of `claude -p`. decisions.md row: reverses row 11 and the mechanism in row 74 (row 12's setup-token note lapses with it); the reasons are the three in this milestone's preamble plus: recipes are public content, so a tier that trains on prompts is free in every sense. Check: runner-injected tests for a fixture answer, a fenced answer and garbage; a fake `fetch` test for each error mapping; a test that an unset key hides the option and a set one shows it; the compose test; the Settings render test updated.
- [x] **! M36.2 Ingredients belong to parts.** `ScrapedPart` becomes `{ name, ingredients: string[], steps: string[] }` and `ScrapedRecipe.ingredients` goes; a part's ingredient lines are the lines under its heading. The schema rung puts every `recipeIngredient` line on the unnamed part, since schema.org cannot say more (row 59's note stands). `MealieRecipe`'s and Tandoor's per-part overrides collapse into the one shape, so `draftFromScraped` reads ingredients from the part rather than from two places. `SCRAPED_JSON_SCHEMA`, `ScrapedRecipeSchema`, `normaliseScraped`, `hasContent`, `stepCount` and the review's ingredient count follow. Check: the existing schema, Mealie, Tandoor and AI tests updated to the shape; the JSON-schema-matches-zod test still holds; a Mealie fixture with sections lands its rows on the right parts.
- [x] **M36.3 Readable page text.** `src/domain/pageText.ts`: `readableText(html)` drops `script`, `style`, `noscript`, `template`, `svg`, `iframe`, `form`, `nav`, `header`, `footer`, `aside` and comments; block elements end a line; `h1`–`h6` come out on their own line prefixed `# `; `li` as its own line; entities decoded through `decodeEntities`; runs of whitespace collapsed; capped at `MAX_AI_TEXT`. Pure, regex and a small tag scanner, no DOM. `ImportedRecipe` gains `pageText: string`, filled by `importRecipeFromUrl` and empty for the file imports. Check: over the recorded 200 fixture from M35.4, the ingredient headings survive as `# ` lines, no script text survives, and the output is under the cap; a test that `<br>` and `</p>` both end a line.
- [x] **! M36.4 The anchored prompt.** `aiPrompt({ text, anchor })` where `anchor` is the schema rung's `ScrapedRecipe` or null. With an anchor the rules change: the anchor's ingredient lines and step texts *are* the recipe (this is the common case now, since M36.6 runs the read on every structured page); copy each exactly once, byte for byte; the text is only for which heading each line sits under and what to name that part; a line under no heading stays on the unnamed part; never add, drop, merge, split or reword a line or a step. The anchor is given as JSON after the text. Without an anchor the prompt is today's. `claudeArgs` and `runAiImport(text, { anchor })` follow, and `MAX_AI_TEXT` applies to text and anchor together. Check: prompt tests that the anchor's lines appear and the rules appear only with an anchor; a runner-injected test that the anchor is passed through.
- [x] **! M36.5 Checking the answer.** `src/domain/importCheck.ts`: `checkAgainstAnchor(answer, anchor)` compares the answer's ingredient lines across all parts with the anchor's as a multiset, and the step texts likewise, after `decodeEntities`, whitespace collapse and trim; returns `{ ok, missingLines, addedLines, missingSteps, addedSteps }`. Pure. `runAiImport` with an anchor runs it: `ok` gives `from: "ai"` with `check` attached; not `ok` returns the anchor's recipe as `from: "schema"` with `check` attached and the answer under `rejected`, so the review can offer it. decisions.md row: on a structured page the JSON-LD owns the content, the model owns the structure, and a mismatch discards the structure, not the content — `recipe_scrapers`' group rule with the selectors replaced. Check: table tests for the comparison (reordered into parts passes; one dropped line fails; a reworded step fails; entity and whitespace differences pass); runner fixtures for the accepted and rejected paths.
- [x] **! M36.6 The model reads every page.** With a model configured, `RecipeSource` runs the read on arrival for both a `schema` result (anchored, over `pageText`) and a `stub` result (unanchored): the rules result renders at once with a "Sorting into parts…" line above the rows, and the review re-renders from the `ai` result when it lands, the summary saying so. If the read fails, the rules result stays with the error inline and a **Try again**. On a rejected answer a `Message` says what changed ("The model's version dropped 1 line and reworded 2 steps, so the page's version is shown") listing the lines, with a quiet **Use the model's version anyway** that swaps `rejected` in. With no model configured the review is today's, and on a `schema` result says that sections were not sorted. The client calls `importFromText` with `pageText` and the anchor, so the server fetches nothing twice; Create is not blocked while the read runs, so a one-list recipe can be saved before the model answers. decisions.md row: the model is the default reader of a page, the rules are its anchor and its fallback (refines row 74's "rung under"). `importSummary` gains the sorted and unsorted cases. `architecture.md`'s import line and `scope.md`'s `claude -p` line are rewritten to the pipeline: fetch, extract, read with an anchor, check, review. Check: render tests for the in-progress line, the swap to the `ai` result, the failure with retry, the rejection message and the swap, the unavailable note; a test that Create works before the read lands.
- [ ] **M36.7 Pasted HTML takes the rules first.** `looksLikeHtml(text)` (a doctype, an `<html`, or an `ld+json` script) in `src/domain/pageText.ts`. `importRecipeFromUrl` splits: the fetch, then `importFromHtml(html, url)` which runs `extractRecipe` and `readableText`; `importFromText` calls the same `importFromHtml` when the paste looks like HTML, then the model over the readable text with the anchor when the page had one, so view-source-and-paste gets exactly what a successful fetch would. Prose is unchanged. The paste box's label gains "or the page's HTML (view source, select all, copy)", and the 403 message from M35.4 says the same. Check: pasting the recorded 200 fixture returns `schema` with `pageText` filled; a paste of prose still goes to `ai`; a paste of the fixture with its `ld+json` cut out returns a stub and then reads.

## Blocked

_(none)_

## Questions

- M36: three cheap additions were left out on purpose and are Jason's call. (a) A microdata reader for pages with `itemprop` attributes and no `ld+json`, a few dozen lines, rules-based, no rot. (b) `recipe.import_source` holding the JSON-LD the recipe was made from, so a better prompt or model later can re-sort without fetching again. (c) A PWA `share_target` so a page shared from the phone lands on the import screen with the HTML the browser already has, which is Tandoor's bookmarklet answer to Cloudflare and would have got Serious Eats past M35.4.
- M35.1: a step photo uploads only once the step exists in the database, so "Add image" on a brand-new step says "save the recipe first". Holding the file until save (as the recipe image does) needs per-step plumbing through RecipeForm; Jason may want that instead.
- M34.4: a Tandoor nested recipe only gets `food.recipe_id` if the child is already in garnish, so the child must be imported first; parent-then-child leaves the food unlinked until "Make this a food" on the child. A bulk import of a whole export would resolve links in one pass (same open question as M34.3).
- M34.3: a Mealie backup zip holding many recipes asks which one to import (one recipe per review pass) rather than importing the collection in bulk. Mealie's own restore is whole-collection; a bulk restore screen would be a separate task if Jason wants it.
- M31.5 is the app's first queued write. The design in the task (local queue, optimistic apply, flush on reconnect, "N changes waiting") is the default; Jason may want a different one before it runs.

## Log

_(one line per iteration: date, task id, outcome, model)_

2026-09-13  —      note   —        —      stashed a one-line uncommitted RecipeCard.tsx change left by a previous run (`git stash -u`)
2026-09-13  M31.1  done   ea86435  opus   005_shopping.sql, Drizzle mirror, repo, domain doc, server functions; architecture Shopping list section built; decisions row 67
2026-09-13  M31.2  done   f797c6b  sonnet mergeIntoList in src/domain/shopping.ts returns a {merges, additions} plan; merges by food+unit onto unticked lines, ticked/free-text/food-less start new lines, skipShopping dropped
2026-09-13  M31.3  done   ee71d57  opus   AddToShoppingSheet at page scale on the recipe page and cook finish card; Add runs mergeIntoList, added repo.mergeInto + mergeShoppingItems for the merges half, toast gained an action; View list uses window.location until M31.4 types the route
2026-09-13  M31.4  done   186c723  opus   /shopping route and nav item; groupByAisle/shoppingItemLabel/sourceLabel pure in domain/shopping.ts; sources as <details>; toast View list now router.navigate
2026-09-13  M31.5  done   3d0b315  opus   src/lib/outbox.ts queues tick/untick/remove in localStorage, coalesced per line, optimistic apply, flushed in order on reconnect or focus; "N changes waiting" header; Add and Clear ticked stay server-first; decisions row 68
2026-09-13  M31.6  done   7f38103  sonnet /shopping loader fetches aisles; unassigned food rows get a quiet Set aisle Select in the expansion writing updateFood via setFoodAisle, invalidated so the row regroups
2026-09-13  M32.1  done   262dd81  opus   006_conversions.sql food_conversion + Drizzle mirror and repo; food document gains conversions, updateFood carries them, setFoodConversions replaces wholesale; FoodEditSheet conversions editor with pure parseConversions; decisions row 69
2026-09-13  M32.2  done   cac23f1  sonnet src/domain/convert.ts over food conversions then unit standard_*, one hop each way; mergeIntoList merges convertible lines into a persisted line's own unit, or the metric one when two fresh additions meet
2026-09-13  M32.3  done   5a56bb3  opus   food.recipe_id editable from the Settings food sheet (Combobox) and the recipe menu (Make this a food); ingredient row links to the child with a Make N servings hint via subRecipeScale + convert; loader fetches children once via listSubRecipes into a SubRecipesProvider; decisions row 70
2026-09-13  M32.4  done   987a7d0  sonnet cook loader fetches sub-recipes and resolves ?from= to the parent name; step card sub-recipe row links into the child's cook mode at derived servings; child's Finished card offers Back to <parent>
2026-09-13  M32.5  done   4f0a3ed  sonnet subRecipeAdditions expands a sub-recipe row into the child's ingredients at the derived servings with the child as source, falls back to the food line; per-row Add <child>'s ingredients instead toggle in the sheet lazily fetching the child
2026-09-13  M33.1  done   f72485b  opus   007_plan.sql meal_plan_entry + Drizzle mirror and repo reading a week as seven PlanDays; src/domain/plan.ts zod doc and UTC date helpers; src/server/plan.ts listPlanWeek/add/update/move/remove; architecture Meal plan section; decisions row 71
2026-09-13  M33.2  done   593bd02  opus   /plan route and nav item; seven columns from md with ?week= Monday; days are ReorderList lists in one drag group plus a row menu; add row reuses SearchResultList extracted from GlobalSearch, Enter makes a plain line; recipe name copied into text; pure label/date helpers in domain/plan.ts
2026-09-13  M33.3  done   441a5d2  sonnet planWeekAdditions in domain/plan.ts over a new exported recipeAdditions; addPlanWeekToShopping server function merges and writes; Add this week to the shopping list button in the /plan header
2026-09-13  M33.4  done   5e24071  sonnet RecipeActions gains Plan opening PlanPopover (next 7 days, NumberStepper defaulting to page scale); choosing a day writes addPlanEntry with the recipe name copied into text
2026-09-13  M34.1  done   04b2273  opus   GET /api/recipes/:slug.json and /api/export.json via file routes export[.]json.ts and {$slug}[.]json.ts over pure domain/export.ts; Settings Export tab; decisions row 72. Agent reports `bun --bun vite build` fails at prerender at HEAD before this task (jsxDEV undefined in RootDocument); unverified by the orchestrator, outside the plan
2026-09-13  M34.2  done   7f9610b  sonnet toCooklang in domain/cooklang.ts (metadata, == Part ==, @food{qty%unit}, = fixed, ~ timers via durationsIn); GET /api/recipes/:slug.cook as text/plain; Copy as Cooklang in RecipeActions; golden files for the three seed recipes and a seeded property test
2026-09-13  M34.3  done   6a15aa8  opus   third source option takes a Mealie backup zip or recipe JSON, parsed purely in domain/importMealie.ts over a dependency-free zip reader (DecompressionStream deflate-raw); sections become parts, lands on the M17.5 review, duplicates warned by name; Tandoor refused until M34.4; POST /api/import/file; decisions row 73
2026-09-13  M34.4  done   05b82f3  opus   domain/importTandoor.ts reads recipe.json, a recipe zip or a zip-of-zips; each step a part with rows linked to its own step; keywords/servings/times/source mapped; nested step is a food row linked via foodForRecipe when the child came too, else a text line; readExport dispatches Mealie vs Tandoor by shape
2026-09-13  M34.5  done   72b9ad7  opus   paste box and AI rung under JSON-LD/OpenGraph gated on Bun.which(claude); src/server/aiImport.ts with an injectable runner (default Bun.spawn with 60 s timeout, not Bun.$ which has none), hand-written --json-schema held to the zod key set by a test; malformed answers raise a typed AiImportError; Settings note on claude setup-token; decisions row 74
2026-09-13  M35.1  done   12522e1  opus   008_step_images.sql adds step.image, carried in the recipe document; POST /api/steps/:id/image and GET /api/images/steps/:file via server/stepImages.ts; step row menu Add/Replace/Remove image; StepCard shows the photo at both sizes
2026-09-13  M35.4  done   300a5be  sonnet two browser header profiles in domain/fetchProfiles.ts, importFromUrl retries once on 403 and names the site suggesting the paste box; recorded 403/200 fixtures. Serious Eats still blocked: Cloudflare managed challenge (JS + TLS fingerprint) on one profile, 402 on the other; TLS fingerprinting stays deferred
2026-09-13  M35.3  done   96b2748  sonnet recipe summary gains ingredientPreview (first six formatted lines, part then row order) from a repo query; RecipeCard wraps its link in a design-system Tooltip shown from md; fixtures updated
2026-09-13  M35.2  done   51dc8a0  sonnet 009_cook_servings.sql adds timeline_event.servings end to end; Made this sheet gains a NumberStepper defaulting to the page scale; History row shows serves N via servingsLabel
2026-09-13  —      note   —        —      M34.1's build report checked: `bun run build` (which sets NODE_ENV=production) prerenders cleanly; the agent had run `bun --bun vite build` bare, and Bun's default NODE_ENV=development makes Vite emit the jsxDEV transform that React's production jsx-dev-runtime does not export. Not a bug
2026-09-14  M36.1  done   b1fa680  opus   AI rung drops the claude CLI for one OpenAI-compatible chat/completions POST configured by AI_API_KEY, AI_BASE_URL (default Gemini) and AI_MODEL (default gemini-2.5-flash), passed through compose, documented in README and Settings; decisions row 75
2026-09-14  M36.2  done   bc8d325  opus   ScrapedPart gains ingredients and ScrapedRecipe.ingredients goes; schema rung puts every line on the unnamed part; Mealie and Tandoor parts become ScrapedParts carrying parsed rows alongside; draftFromScraped derives each row's part from the parts' line counts, rowParts removed
2026-09-14  M36.3  done   a6dd959  sonnet src/domain/pageText.ts readableText and MAX_PAGE_TEXT (MAX_AI_TEXT derives from it); ImportedRecipe.pageText filled by extractRecipe, empty for file and AI imports. The M35.4 200 fixture is an 847-byte stub with no headings, so the Check ran over a new synthetic WPRM-style fixture sectioned-page.html instead
2026-09-14  M36.4  done   44b99f0  opus   aiPrompt({ text, anchor }) sorts the JSON-LD's own lines into parts instead of re-reading them; anchorJson is the compact anchor block (keeps image and description so an accepted answer keeps the page's image); runAiImport(text, { anchor }) and ImportFromTextInput.anchor carry it; MAX_AI_TEXT measures text and anchor together
2026-09-14  M36.5  done   98e2cf6  opus   src/domain/importCheck.ts checkAgainstAnchor compares lines and steps as multisets after entity decode and whitespace collapse; anchored runAiImport keeps a passing answer as ai with check, returns the anchor as schema with the answer under rejected when it fails; decisions row 76
2026-09-14  M36.6  done   9b18595  opus   RecipeSource hands every URL result to the model on arrival; shouldReadWithModel/modelPass/withRejectedAnswer pure beside the review; loadText grew an anchor argument; review gained reading, readError, Try again and Use the model's version anyway; importSummary gained a sorted flag; decisions row 77; architecture and scope rewritten to fetch/extract/read/check/review
