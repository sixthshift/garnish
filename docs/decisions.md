# Decisions

One line each. Newest at the bottom. Reverse a decision by adding a new row, not editing the old one.

| # | Decision | Chosen over | Why |
|---|---|---|---|
| 1 | Build, not deploy | Mealie, Tandoor, cookcli | None has components as real objects with a phone-first UI. cookcli comes closest but its import is OpenAI-only and its UI is secondary |
| 2 | Bun | Node | Single runtime for server, bundler, SQLite, tests |
| 3 | SQLite is the only store | Cooklang files; files + SQLite index; Postgres | Robust and versatile as features grow. Files can't enforce references. One writer, so Postgres buys nothing |
| 4 | No file export in v1 | Continuous git-tracked export | Recipes are mastered in the DB. Export is a later feature, not a design constraint |
| 5 | React + Tailwind via Bun HTML imports | Svelte, HTMX, vanilla | Largest ecosystem for PWA work. No Vite |
| 6 | No auth, no users | Single household login; per-member accounts | LAN only. Nothing in v1 needs identity. Meal plans and lists are household-wide |
| 7 | Mealie schema outside recipe internals | First-principles design | Years of settled decisions in a public OpenAPI spec |
| 8 | Tandoor shape inside recipes: components own ingredients | Mealie flat array with a `title` flag | A flag is cosmetic and the parser drops it. A table can't be bolted on later |
| 9 | Cooklang as a reference, not a store | Cooklang as source of truth | Its sections, `=` fixed quantities, servings scaling and aisle config are borrowed. Its files are not |
| 10 | No AI in v1 | Claude import via `claude -p` | Library and scaling first. Schema is designed so import returns the same document the editor saves |
| 11 | When AI arrives, it runs as `claude -p` on subscription | Anthropic API key; Agent SDK | Subscription tokens. Agent SDK is API-key only. Verified against Claude Code docs 2026-09 |
| 12 | Docker, single container, one volume | Bare Bun on a host | Portable. `claude -p` in a container will need a `claude setup-token` injected |
| 13 | Shopping list merges by food and unit, expandable to sources | Never merge; merge without breakdown | Usable list, provenance kept |
| 14 | Recipe-to-recipe references deferred | Track live; pin; ask on edit | Open. Schema leaves room via `component.ref_recipe_id` |
| 15 | Scaling computed on read | Persist scaled copies | Nothing to keep in sync |
| 16 | Quantities stored as decimals | Fraction strings | Arithmetic is exact enough; fractions are display |
| 17 | Mealie is the tie-breaker | Deciding from scratch | When unsure, do what Mealie does |
| 18 | UI is `@sixthshift/design-system` | Raw Tailwind; shadcn direct | Personal design system. Missing primitives built locally in garnish, promoted later if generic |
| 19 | Sub-recipe link lives on `food`, not `ingredient` | `ingredient.ref_recipe_id`; `component.ref_recipe_id` | An ingredient line shouldn't care whether hollandaise has a recipe. Tandoor's `Food.recipe`. Supersedes 14's slot |
| 20 | Servings and yield: two numbers plus a string | Free text only | Mealie's `recipeServings`, `recipeYieldQuantity`, `recipeYield`. Numbers scale, string displays |
| 21 | Ingredient `quantity`, `food`, `unit` nullable; `original_text` kept | Required fields | Mealie does this. Parsing is never complete; text-only lines must save |
| 22 | Component holds ingredients and steps as two lists | Tandoor's one step = one ingredient group | Cook view shows one short step at a time. Editor cost accepted |
| 23 | Aisle is a table; `food.skip_shopping` flag | Aisle string on food | Mealie labels and Tandoor supermarket categories are both tables. Tandoor's `ignore_shopping` |
| 24 | zod for validation | TypeBox, Valibot | Nearest to Mealie's pydantic. One schema per document |
| 25 | UUID ids, recipe slugs | Integer ids | Mealie |
| 26 | Images on the volume, not in SQLite | Blobs | Mealie stores files on disk |
| 27 | Metric, en-AU default; seed units only | Imperial; seed foods from Mealie | Household preference. Foods accrue from use |
| 28 | TanStack Start is the framework | Bun HTML imports + hand-rolled `Bun.serve` routes | Jason's call. File routing, typed server functions, SSR/SPA toggle. Supersedes 5's "no Vite": Vite runs via `bun --bun`, Nitro Bun preset in prod. RC status accepted |
| 29 | SPA mode | Full SSR | LAN app, no SEO. Prerendered shell plus cached data is the standard offline PWA shape |
| 30 | Server functions for app calls, server routes only under `/api/*` | REST for everything | Type safety end to end. `/api/*` kept for health, images, and future external import |
| 31 | vitest via `bun run test` | `bun test` | One runner shared with the Vite toolchain; the gate is the same command everywhere. Never `bun test` |
| 32 | Bun ≥ 1.4 for build and runtime; `NODE_ENV=production` set in the build script | Stay on 1.3.9; patch bundler output | 1.3.9's JS engine fails to parse the `else a: if` construct rolldown emits for react-dom/server, so the built server 500s and the SPA shell never prerenders; 1.4.2 parses it. Bun defaults `NODE_ENV` to development, which makes Vite ship the dev JSX runtime in a production build |
| 33 | `test` script is `bun --bun vitest run` | Plain `vitest run`; a Node SQLite shim for tests | vitest's bin and its worker pool run under Node by default, where `bun:sqlite` does not resolve. `--bun` keeps the runner and the store the same as production. The gate is still `bun run test` |
| 34 | `@tanstack/start-storage-context` as a dev dependency | Re-implement the server-function test path; call handlers as plain functions | Under vitest the Start compiler splits each server function into a stub plus a split module, and running the split export needs a Start request context. `runWithStartContext` is only exported from this transitive package, so it is pinned to the version Start already installs. Tests only; the app never imports it |
| 35 | `prepTime` and `performTime` are integer minutes under Mealie's names | Mealie's free-text strings; renaming to `prepMinutes`/`cookMinutes` | Numbers total and format without parsing; keeping Mealie's names lets a future import map fields 1:1. The rename is an open question in plan.md |
| 36 | `NotFound` mapping is a `notFoundMiddleware` in every `createServerFn` chain | A `defineServerFn` wrapper with the validator and error mapping built in (plan M3.1) | Start's compiler extracts a server function by finding the literal `createServerFn(...)...handler()` chain at top level; hidden behind a wrapper it is never split out. Each function is written in full and shares the middleware |
| 37 | Hand-written service worker in `src/sw/`, emitted as an asset of the client build by a local Vite plugin | workbox or vite-plugin-pwa; a post-build script writing `.output/public/sw.js` | No new dependency for four fetch policies. Nitro serves static files from a manifest baked into the server bundle, so a file written after the build is a 404; emitting during the client build puts it in the manifest and hands the plugin the built asset list for the precache |
| 38 | Migration SQL inlined into the server bundle with `import.meta.glob(?raw)` | Copy `src/db/migrations` into the runtime image and read it from disk | The built `.output/` has no source tree; bundling keeps the runtime stage to `.output/` alone. The `bun run migrate` CLI still reads the directory |
| 39 | `nitro()` left out of the Vite config when `VITEST` is set | A module shim; running tests through a nitro dev server | Combined with `tanstackStart` it inlines React's CJS build into the module runner ("module is not defined"). Tests call handlers and server functions directly; nitro is only the deploy adapter |
| 40 | Recipe editing stays on `/recipes/$slug/edit` | Mealie's edit-in-place on the view page (`?edit=true`) | Working route already; in-place is a routing rewrite for no phone benefit. Mealie's discard guard and sticky Save are copied anyway |
| 41 | "Made this" as timeline events: `timeline_event` (recipe, date, message?, image?) that also sets `recipe.last_made` | `last_made` only; Tandoor's cook log with per-cook rating and servings | Mealie's shape. History without a second rating system. Feeds last-made sort and the cook mode's finishing card |
| 42 | Tags are the only organiser | Mealie's categories and tools beside tags | A category is a second tag namespace; tools matter only to shopping and on-hand, both Later or Never |
| 43 | `recipe.favourite` boolean | No favourites; a favourites tag | Mealie has it on the card and as a filter; a flag is one column and avoids a magic tag |
| 44 | Global search dialog (M12.5) built on `Modal`, not a `Sheet`/`Modal` switch | A literal `sheet` on phone and `Modal` on wide, chosen via `useDeviceFormFactor` | `Modal`'s own mobile shape already slides up full-width from the bottom (a sheet in effect) and is centred from `md` up — the plan's "sheet on phone, centred on wide" in one component, no second dialog mounted alongside it |
| 45 | `ConfirmDialog` (M13.6) stays on `Modal` unchanged, size `sm`, default (centred) align | A literal `Sheet` below `md` and `Modal` from `md` up | Same shape as decision 44: `Modal` already renders full-width from the bottom below `sm` and centred from `sm` up, which is the "sheet on phone, centred on wide" the task line asks for; `ConfirmDialog` (and `UsageConfirmDialog`, built on it) already used `Modal` this way before this task. No second dialog primitive, no new breakpoint logic |
| 46 | Repositories query through Drizzle ORM over the same `bun:sqlite` handle; `src/db/models/` is one folder per domain, each with `schema.ts` (its tables) and `repo.ts` (its repository) | Hand-written SQL with hand-declared row types (what stage 1 and 2 shipped); drizzle-kit owning migrations too | Row types are derived from the schema instead of restated per repository, so a column rename is a type error rather than a runtime surprise. Not a store change — Drizzle wraps the connection `openDatabase` already returned, and `migrations/*.sql` with `migrations/migrate.ts` still build the database, because an existing volume's `migration` rows must keep meaning what they say. `test/db/drift.test.ts` holds the mirror to the SQL; `bun run db:generate` writes to a scratch directory to be reviewed and copied, since Drizzle cannot express `COLLATE NOCASE` |
| 47 | Ingredient parsing is a pure rules-based matcher over the `unit` and `food` tables (`src/domain/parseIngredient.ts`), run behind a review step before any row is created | `claude -p` parsing each line; auto-creating unmatched foods; fuzzy (edit-distance) food matching | The vocabulary is already in the database — unit name/plural/abbreviation, food name/plural/aliases — so this is longest-match lookup, not language understanding, and it runs offline and instantly on every keystroke path. `format.ts`'s `formatIngredient` is the exact inverse, so a round-trip property test gates correctness. AI stays on the import path (whole page to whole document), which is Later. The review step is Mealie's: auto-create fills the food table with near-duplicates ("flour", "plain flour", "Plain Flour") that then need the merge tools. Fuzzy matching and learning from corrections are deferred until the plain matcher has been used |
| 48 | `component` is renamed `part` everywhere: the table, `ingredient.part_id`, `step.part_id`, the document's `parts`, and the editor's `PartsEditor` | Keeping `component`; `section` (Cooklang's word, and the one CLAUDE.md uses); `stage`; `group` | `component` collides with React components in every other file of a TanStack Start app — `src/components/` beside `db/models/recipe/component` is a name you trip over for the life of the project. `part` reads as what the thing is, a thing the recipe makes, rather than as layout (`section`) or sequence (`stage`); it also leaves `sub-recipe` free for `food.recipe_id` (row 19), which is a different concept. Names the schema comment already used ("a named part of a recipe") |
| 49 | Every step belongs to a part: `step.part_id` is NOT NULL, `step.recipe_id` is dropped, `position` is unique per part, and the document has no recipe-level `steps` array. The unnamed part (`name = ''`) is the recipe's main body | Row 22's recipe-level steps beside per-component steps; naming the main body "To assemble" or similar so it could be a named part | Two containers for one concept: a step could sit in a component or float on the recipe, and the recipe-wide `position` that ordered them was derived on every write and discarded on every read — it encoded an unwritten rule (loose steps always last) that the editor could not express. One container removes the rule and the column. The main body keeps its ingredients, which a recipe-level `steps` array had nowhere to put, and renders without a heading, so a recipe with one sub-preparation is not forced to invent a name for the rest of its method. Supersedes the loose-steps half of row 22; the two-lists-per-part half stands |
| 50 | The editor mirrors the view page's order — image and name, then the parts with their ingredients and steps, then notes — and everything else (yield, times, tags, source URL) moves behind a `Details` disclosure that opens closed on a new recipe | The metadata-first column stage 2 shipped (image, name, description, servings, rating, yield, times, tags, then the parts); Tandoor's tabs (Recipe / Steps / Properties / Miscellaneous) | Six controls stood between "New recipe" and the first ingredient, and none of them is what you have in front of you when you sit down to type a recipe in. Mirroring the view page means nothing moves when you switch between reading and editing, which is most of what Mealie's edit-in-place buys without the routing rewrite decision 40 declined. Tabs solve the same problem but put the meta somewhere you have to remember to visit; a disclosure that opens itself when it holds anything cannot be forgotten |
| 51 | Rating and last made are not editable in the recipe form; they are set from the view page and the timeline | Keeping the `Rating` control in the editor beside servings | Decision 41 already made "Made this" the thing that records a cook and writes `recipe.last_made`, so the form offered a second way to set a field one action already owns. Rating a recipe on the screen where you are first typing it in is rating something you have not cooked. The document still carries both fields and an edit round-trips them untouched — this is about which screen offers the control, not about the schema |
| 52 | `/recipes/new` opens on a paste box: `splitRecipe` divides a pasted block into ingredient lines and step lines, each ingredient line goes through `parseIngredient`, and the whole thing is reviewed before it becomes a draft | Opening on the blank form (what stage 2 shipped) with per-list bulk add as the only paste path; waiting for the `claude -p` import scope.md lists under Later; scraping a URL | Every piece already exists — `parseIngredient` (row 47), `paragraphs`, and M17.5's review row — and none of them is composed over a whole recipe, which is the shape a recipe actually arrives in. Rules-based keeps it offline, instant and deterministic, and it does not spend the AI import's budget early: when `claude -p` lands it replaces `splitRecipe`, not the review step, which is the part worth keeping either way. Blank stays one click away for the recipe you are writing rather than copying |
| 53 | A recipe whose only part is unnamed renders no part chrome in the editor: no name input, no card, no reorder handle, no "Parts" heading — just "Ingredients" and "Steps". "Add part" reveals the chrome | Always rendering the part card, which is what M18 shipped; Mealie's per-row "toggle section", where structure is created from an ingredient's or a step's own menu | The view page has printed the unnamed part without a heading since row 49, and the editor contradicting it made the common case — a flat recipe — look like a structured one with a blank label. Mealie's per-row toggle is the more elegant idea but it fits a flat ingredient array with a `title` field, not a parts table that owns two lists; promoting a whole part is the operation this schema actually has |
| 54 | Row-level actions live in one `⋮` `Menu` per row; list-level actions (bulk add, split all, merge all, parse all) live once in the section header | The inline button row per step stage 2 shipped — insert above, insert below, split by paragraph, merge with next, rendered under every textarea | Four buttons times ten steps is forty controls for a list whose content is ten sentences, and on a phone they wrap to three lines under each step. Both incumbents landed on the same split: Mealie's step `dotsVertical` menu carries seven entries, Tandoor's step and ingredient menus carry four or five, and Tandoor's split/merge/auto-sort sit once above the list. The header is also where a "Parse all" banner belongs, which per-row Parse (M17.6) had nowhere to go |
| 55 | Enter appends and focuses the next row when pressed on the last row of an ingredient or step list, with `preventDefault` so the form is not submitted; from any earlier row it moves focus to the next row | Tandoor's Tab-out-of-the-last-field-appends; leaving "Add ingredient" as the only way | Typing a recipe is a list of rows, and a mouse trip per row is the difference between ninety seconds and four minutes. Tab is the browser's own focus order and stealing it on the last field means there is no longer a way to tab out of the list, which matters more here than it does in Tandoor's modal. Enter inside a single-line input in a form submits it by default, so this takes an explicit `preventDefault` either way |
| 56 | Steps get a per-row markdown preview toggle in the row menu, rendering `Markdown.tsx` in place of the textarea | No preview, the bare textarea stage 2 shipped; a permanent side-by-side preview | Steps render as markdown on the view page and in cook mode, and the editor gave no way to see it, so a mis-typed list or a stray underscore was only ever found after saving. Mealie has the same toggle in the same place (`preview-step` in the step's menu); side-by-side does not fit a phone, which is the width this is designed for |
| 57 | Reverses row 52. Pasting recipe text is not a way into a new recipe. `/recipes/new` opens on a source chooser — **a web page**, or **my own** — and `src/domain/splitRecipe.ts`, `RecipeImport` and the part editor's Paste button are deleted with it | Keeping paste as a third source; keeping `splitRecipe` unexposed to serve the URL import's no-structured-data case | The first question about a new recipe is where it came from, not how the text will reach the form — and "pasted text" answers the second, so it sat oddly beside "a web page". Deleting rather than hiding it: a code path reachable only as someone else's fallback is a path nobody maintains, and neither incumbent has one (Mealie's equivalent rung is AI, Tandoor's is an error). Prose extraction comes back with `claude -p`, where it belongs — scope.md has it under Later and row 52's review step survives regardless, because bulk add (M13.4), row parse (M17.6) and Parse all (M21.3) all still use it |
| 58 | The URL import is rules-based: schema.org Recipe in a page's `ld+json`, falling back to an OpenGraph stub — name, description, image, source — with empty lists. No per-site scrapers, no AI | `recipe_scrapers`' ~500 per-site classes, which both incumbents run first; Tandoor's hard "No usable data could be found" with no fallback; waiting for `claude -p` | intent.md names "scrapers rot per site" as one of the holes this project exists to avoid, and per-site classes are exactly that. JSON-LD is one standard nearly every recipe site emits and is what `recipe_scrapers`' own wild mode falls back to, so it is the non-rotting part of what the incumbents do. The OpenGraph rung is Mealie's `RecipeScraperOpenGraph`, and it is honest: it does not pretend to have found a recipe, it gets you a named, illustrated, linked shell to type into — better than Tandoor's error and reachable without an AI budget. Ingredient lines still go through `parseIngredient` and row 47's review, so nothing is created that was not approved |
| 59 | A schema.org `HowToSection` in `recipeInstructions` becomes a garnish part, named from the section | Flattening every section into one list of steps, which is what Mealie does because its recipe has no parts | The shapes already match: a section owns an ordered list of steps and has a name, which is a part minus its ingredients (row 48, row 49). Flattening would throw away structure the page had and the document can hold, and it is the one place garnish's model is richer than the incumbents' rather than merely different. Ingredients stay on the unnamed part — schema.org has no way to say which section an ingredient belongs to |
| 60 | The ingredients aside carries its own "Ingredients" `SectionTitle` with `ScaleControl` in the same heading row, and `IngredientModeToggle` renders only when the recipe has more than one part, labelled "One list" | Leaving the scale control and the toggle in a loose row between the page header and the grid, which is what M24.1 shipped; keeping the toggle's old "Summary" label and its `hasIngredients` guard | Mealie's ingredient list header pairs the title with the servings stepper in one row; the loose row was a stopgap `md:sticky` couldn't fix a name for. A switch labelled "Summary" reads as a mode with no opposite once a flat recipe is the only recipe on screen — "One list" says what checking it does, and gating on part count rather than on whether any list is non-empty is the same condition the aside's per-part headings already use |
| 61 | Notes sit directly under the recipe header, above the ingredients/method grid, and `NotesEditor` renders before `PartsEditor` to match | Leaving notes after the steps and `TimelineList`, which is what stage 4 shipped | Notes are the household's amendments — "use half the sugar", "double the garlic" — and are only useful read before cooking starts, not after the method and the cook log have already scrolled past. Decision 50 already ties the editor's order to the view page's, so moving one moves both |
| 62 | Servings scaling happens on the client: the view and cook loaders fetch the stored document once and each page applies `scaledForServings` from the `?servings=` search param | Keeping `servings` in `loaderDeps` so every − and + refetched the scaled document, which is what stages 2 and 3 shipped | `scaleRecipe` is pure and already in `src/domain/`, so the round trip bought nothing but latency: a tap on + waited on the network to multiply numbers the browser already had. The URL still carries the scale, so a refresh, a share and cook mode's hand-off are unchanged, and `getRecipe({ servings })` keeps scaling for outside callers (`/api/*`, a future import) — one pure function now serves both sides, so they cannot drift |
| 63 | An empty ingredient list is a `Textarea`, one ingredient per line, whose **Add** runs the bulk-add review inline in place of the textarea; the structured row is the correction view, not the entry view. Once the list has rows the textarea goes and "Bulk add" stays in the header | Keeping "No ingredients yet" plus an "Add ingredient" button that opens one blank structured row, which is what stages 2-4 shipped and what Tandoor does; making the bulk sheet the only paste path | Typing a recipe out is how a recipe arrives — off a page, a photo, a text message — and the first thing the editor asked for was a quantity field, a unit combobox and a food combobox for a line the cook could have typed whole. Mealie's ingredient editor has the same shape of escape hatch, a text mode that parses into rows, and row 47's review already exists to make parsing safe; this makes that path the front door rather than a sheet behind a button. Nothing about the commit changes: `ingredientReview` and `confirmReviewedIngredients` are shared with the sheet, so a declined food still lands a text-only row and nothing is created that was not approved |
| 64 | Steps link ingredients: a `step_ingredient` table, many to many, **within one part only**. A step may link any ingredient of its own part, an ingredient may be linked from several of that part's steps, and the document carries the links as `step.ingredientIds` in link order. A link naming a row outside the step's part is dropped on save, not rejected | Inferring the links by matching food names in the step text at read time; hanging the ingredient off the step instead of the part (one step owns it, so a row used twice is two rows); allowing a link to reach across parts; splitting one ingredient's amount across the steps that use it | Ownership and use are different questions and the tree only answers the first: where a row lives says how much to buy and what the prep list gathers, and it cannot also say which steps reach for it — butter lives in the pastry and is used in pastry step 1 and pastry step 4. Storing the links rather than inferring them means the cook sees what the household decided, not what a matcher guessed this morning; M28.2's matcher becomes a suggestion the editor accepts, which is row 47's review pattern again. Same part only because a step that needs another part's ingredient is a sign the row belongs in this part or in the recipe's body, and dropping such a link is the same forgiving-write rule the rest of the repository follows — a save is never lost over a stale reference. A link carries the whole row: a genuinely split amount is two rows, which keeps scaling and the shopping list summing one number per row |
| 65 | Cook history is a closed `Disclosure` titled "History" holding compact rows — date, comment truncated on the same line, a `size-12` thumbnail, delete in a row menu | Keeping `TimelineList`'s stacked card per cook (stage 5), open by default, with a large photo and its own Delete button | History is read rarely — logging a cook and reading the next step are the frequent paths, not scrolling last month's comments — so it opens folded and costs one row of screen per cook rather than a card's worth. The count in the hint ("4 cooks") answers "has this been made much" without opening it at all |
| 66 | One `StepCard` is the whole of a step's presentation, on the recipe page and in cook mode: a quiet bordered card holding the number, the ingredients the step links as `IngredientRow`s, the text, and a footer of one `TimerChip` per duration named (deduplicated by length). A `size` prop, `page` or `cook`, is the only difference between the two | Keeping `StepRow` for the page and a separate large-type card in `CookCardView`, which is what stage 5 shipped; name-matched `StepIngredientChips` under the step; timer chips spliced into the prose through `Markdown`'s `decorate` | A cook reads a step the same way whether the page is open or the deck is; two components meant every change to what a step shows had to be made twice and drifted anyway — the page chipped its ingredients only below `md` while cook mode chipped them always. One card makes the page the deck laid out and cook mode the deck one at a time, and `size` is the honest difference: type scale, not content. The ingredients are `IngredientRow`s rather than chips because a linked row is the same row as the prep list's, ticks and all, and a row linked from two steps is meant to appear on both cards. Timers move to a footer because a chip spliced mid-sentence breaks the sentence a cook is reading and gives one duration two identities if the text repeats it; deduplicating by length means "rest 20 minutes, then 20 minutes more" offers one timer, which is what a cook would start |
| 67 | One shopping list for the household, in `shopping_item`: no owner, no list table, no list id in any signature. A line is a food line (`food_id`, `unit_id?`, `quantity?`) or a free-text line, and every line keeps its provenance in `shopping_item_source` — `recipe_id` while the recipe lives, plus copied `recipe_name`, `part_name`, `servings` and the `quantity` that source contributed | Mealie's many named lists with an owner; a single `list_id` column kept "for later"; joining the sources back to `recipe` and `part` for their names; storing only the merged total and dropping where it came from | There is one household and no auth (CLAUDE.md), so a list id would be a column that only ever held one value and a question every screen had to ask and answer the same way; a second list is a migration away if the answer ever changes. Provenance is the thing the list is asked about at the shelf — "why am I buying 350 g of butter" — and the honest answer has to survive the recipe being renamed, or deleted after the list was written, which a join cannot do. So the names are copies and `recipe_id` is only the live link, set null with the recipe: the expansion still reads "Lemon tart, Pastry, serves 4", and offers to open the recipe when there is still one to open. Mealie's `recipeReferences` is the same shape minus the copies |
| 68 | The shopping list is the one screen whose writes queue offline: a tick, an untick and a remove go into `src/lib/outbox.ts` — a `localStorage` queue, coalesced per line, applied to the rendered list at once and flushed in order when `useOnline` turns true or the page regains focus, with a failed entry and everything behind it retained and "N changes waiting" in the header. Adding a line and clearing the ticked stay server-first, and `RecipeForm`'s "changes cannot be saved offline" is unchanged everywhere else | Queueing every write in the app (a general outbox for recipe saves too); refusing ticks offline as the editor refuses saves, leaving the cached list read-only at the shelf; last-write-wins sync with a conflict UI | The supermarket is not on the LAN and the list is the one thing garnish is asked to do while standing in one, so a read-only cached list is a list that cannot be used where it matters. The three queued writes are single-field, id-addressed and idempotent over rows that already exist, which is why replaying them minutes later is safe: the worst case is that a line the cook ticked was also ticked at home, and the answer is the same either way. A recipe save is the opposite — a whole document, replayed over whatever the row has become — so the editor's refusal stands and there is no general outbox to maintain. Coalescing per line means a mind changed twice is one write, not three, and stopping the flush at the first failure keeps a tick and the untick behind it in the order they were made. Add and Clear ticked create and destroy rows and are not what anyone does at a shelf, so they stay refused offline and the exception stays as small as it can be |
| 69 | Conversions live on the food, in `food_conversion (food_id, unit_id, quantity, to_unit_id, to_quantity)` — "1 cup of plain flour is 125 g", one row per food per pair of units, both units cascading. `unit.standard_quantity` / `unit.standard_unit_id` stay exactly as they are and keep the unit-to-unit conversions that hold whatever is being measured (1 l is 1000 ml). The food document carries `conversions: []`, edited in the Settings food sheet as rows of quantity, unit, equals, quantity, unit | Mealie's per-unit conversion alone, which is all `unit.standard_*` is; a global density table keyed by food and unit outside the food; storing grams-per-unit as a single number on the food; deriving the reverse direction as a stored second row | A cup of flour is 125 g and a cup of sugar is 220 g, so the fact being recorded is a property of the ingredient and a conversion hung on "cup" could only ever be right for one food. Unit-to-unit is the opposite — a litre is 1000 ml with anything in it — so the two columns are complementary and neither replaces the other; M32.2's `convert` tries the food's rows first and falls back to the unit's. A pair of quantities rather than a grams-per-unit number means the row reads the way a cook says it and does not privilege grams: "2 tbsp is 30 ml" is the same row shape. The reverse direction is arithmetic, so `convert` derives it rather than asking the household to type both, and the UNIQUE is per direction so an explicitly stored reverse is still legal. This is the piece the shopping list needs before sub-recipes (M32.2): without it a cup of flour and 300 g of flour are two lines |
| 70 | A food may be made by a recipe (`food.recipe_id`, editable at last): the Settings food sheet gains "Made by a recipe", the recipe's action menu gains "Make this a food", and an ingredient row whose food has a recipe renders that food as a **link** to the child with a quiet "Make N servings" hint under it, derived by `subRecipeScale` from the child's `recipeYieldQuantity` / `yieldUnit` through row 69's `convert`. The parent never inlines the child's ingredients or steps | Mealie's and Tandoor's inlining, where a sub-recipe's rows are folded into the parent's list at read time; a disclosure on the row that expands the child in place; storing the derived servings on the ingredient; refusing the link when no scale can be derived | A sub-recipe is a recipe, and the household already knows how to read one — a link takes the cook to a page with its own servings control, its own cook mode and its own shopping button, none of which an inlined block could offer. Inlining also makes one page answer for two: a change to the hollandaise silently rewrites every recipe that folds it in, and the cook cannot tell which numbers are whose. The hint is the one thing a link cannot say on its own, so it is computed, not stored: stored, it would be wrong the moment either recipe's yield changed, and derived it is simply absent when the two amounts cannot be related — "300 g of pastry" against a child that yields "12 tarts" gets the link and no number, which is honest. The link stays in that case because knowing where the pastry comes from is most of the value; only the arithmetic is missing. The row's body stops being the tick toggle when it holds a link (an anchor inside a button is not markup), so a sub-recipe row is ticked by its checkbox alone |
